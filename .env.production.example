# Backend Production Environment (NO subir a GitHub - solo ejemplo)
# Copia este archivo a .env.production y genera valores seguros

# Database
DB_PASSWORD=tu_password_seguro_aqui

# Strapi Secrets (genera con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
APP_KEYS=genera_key_segura_1,genera_key_segura_2
API_TOKEN_SALT=genera_salt_seguro
ADMIN_JWT_SECRET=genera_secret_seguro
JWT_SECRET=genera_jwt_secret_seguro

# CORS - URLs permitidas
CLIENT_URL=http://149.50.132.34
